//collect an array of 2d points based on mouse movements

function mouseDragged() {
  //alert(mouseX);
  console.log(mouseX);
  //ellipse(mouseX, mouseY, 5, 5);
  // prevent default
  return false;
}
